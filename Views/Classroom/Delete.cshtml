@model UniversityAffairs.Models.Classroom

@{
    ViewData["Title"] = "Delete Classroom";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <h2 class="mb-4 text-danger">🗑️ Delete Classroom</h2>

    <p class="lead">Are you sure you want to delete this classroom?</p>

    <div class="card border-danger shadow-sm mb-4">
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3">Room Name</dt>
                <dd class="col-sm-9">@Model.RoomName</dd>

                <dt class="col-sm-3">Capacity</dt>
                <dd class="col-sm-9">@Model.Capacity</dd>
            </dl>
        </div>
    </div>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Yes, Delete</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">Cancel</a>
    </form>
</div>
